# Bearer token for NavAPI (legacy + /api/qr-geo). Do not commit real values.
# Copy to .env next to docker-compose and set a strong secret:
#   openssl rand -hex 32
NAVAPI_API_TOKEN="A0473A67-27A1-3C05-A2D1-5C134639347F"

